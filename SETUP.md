# 🚀 Инструкция по запуску для заказчика

## Быстрый старт (5 минут)

### 1. Создание бота в Telegram

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например: "Feedback Bot")
4. Придумайте username (например: "company_feedback_bot")
5. **Сохраните токен** - он понадобится для настройки

### 2. Получение Chat ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему любое сообщение
3. **Сохраните ваш Chat ID** - он понадобится для получения отчетов

### 3. Настройка проекта

1. **Откройте файл `.env.example`**
2. **Скопируйте его в `.env`** (создайте новый файл с именем `.env`)
3. **Заполните параметры:**

```env
# Токен бота от @BotFather
BOT_TOKEN=ваш_токен_от_botfather

# Chat ID менеджера для отправки отчетов
MANAGER_CHAT_ID=ваш_chat_id

# Время ежедневного опроса (в формате 00:00, по МСК)
SURVEY_TIME=17:00

# Время отправки отчета (в формате 00:00, по МСК)
REPORT_TIME=21:00
```

### 4. Запуск

**Просто запустите файл `run.bat`** - он сделает всё автоматически:
- Проверит Python
- Создаст виртуальное окружение
- Установит зависимости
- Запустит бота

## ✅ Проверка работы

После запуска:

1. **Найдите вашего бота** в Telegram по username
2. **Отправьте команду** `/start`
3. **Попробуйте тестовый опрос** командой `/test`
4. **Проверьте административные команды:**
   - `/report` - получить отчет
   - `/stats` - статистика
   - `/help` - справка

## 📋 Что происходит автоматически

- **Каждый день в 17:00 МСК** - бот отправляет опрос всем сотрудникам
- **Каждый день в 21:00 МСК** - вам приходит отчет в личные сообщения
- **Все данные сохраняются** в папке `reports/` в формате CSV для Excel

## 🔧 Настройка для команды

### Добавление сотрудников

Каждый сотрудник должен:
1. Найти бота по username в Telegram
2. Отправить команду `/start`
3. Готово! Теперь он будет получать ежедневные опросы

### Изменение времени

Отредактируйте файл `.env`:
- `SURVEY_TIME=09:00` - опрос в 9 утра **по МСК**
- `REPORT_TIME=18:30` - отчет в 18:30 **по МСК**

После изменения перезапустите `run.bat`

**Важно:** Все время указывается в московском часовом поясе (МСК, UTC+3)

## 📞 Поддержка

Если что-то не работает:

1. **Проверьте токен бота** - он должен быть правильным
2. **Проверьте Chat ID** - он должен быть числом
3. **Убедитесь, что есть интернет**
4. **Посмотрите на ошибки** в окне с ботом

## 💡 Полезные советы

- **Не закрывайте окно** с ботом - он должен работать постоянно
- **Запускайте на сервере** или компьютере, который не выключается
- **Делайте резервные копии** папки `data/` с данными
- **Проверяйте папку `reports/`** - там сохраняются CSV файлы для анализа

---

**Готово!** Ваш бот для сбора обратной связи настроен и готов к работе! 🎉