# Telegram-бот для сбора ежедневной обратной связи

Бот для автоматического сбора обратной связи от сотрудников о рабочем дне и фокусе задач.

## Возможности

- 📅 Настраиваемое время ежедневного опроса
- 😊 5 вариантов оценки дня (смайлики)
- 📝 Сбор информации о проектах/задачах
- 📊 Автоматические отчеты по расписанию
- 💾 Экспорт данных в CSV для Excel
- 🔗 Простое подключение по ссылке

## Установка и настройка

### 1. Клонирование и установка зависимостей

```bash
# Активируем виртуальное окружение
venv\Scripts\activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
copy .env.example .env
```

Отредактируйте `.env`:
```
BOT_TOKEN=ваш_токен_от_BotFather
MANAGER_CHAT_ID=chat_id_менеджера
SURVEY_TIME=17:00
REPORT_TIME=21:00
```

**Настройка времени:**
- `SURVEY_TIME` - время ежедневного опроса (формат HH:MM, по МСК)
- `REPORT_TIME` - время отправки отчета (формат HH:MM, по МСК)
- Примеры: `09:30`, `18:45`, `12:00`

### 3. Получение токена бота

1. Напишите @BotFather в Telegram
2. Создайте нового бота командой `/newbot`
3. Скопируйте полученный токен в `.env`

### 4. Получение Chat ID менеджера

1. Напишите @userinfobot в Telegram
2. Скопируйте ваш Chat ID в `.env`

## Запуск

```bash
# Запуск бота
python src/bot.py

# Или используйте готовый скрипт на Windows
run.bat
```

## Использование

### Для сотрудников
1. Перейти по пригласительной ссылке бота
2. Нажать `/start`
3. Ждать ежедневный опрос по расписанию

### Для менеджера
- `/report` - получить отчет вручную
- `/stats` - посмотреть статистику
- `/schedule` - посмотреть текущее расписание
- Автоматические отчеты приходят по расписанию из `.env`

## Настройка времени

Время опроса и отчетов настраивается в файле `.env`:

```env
SURVEY_TIME=17:00  # Время опроса сотрудников (МСК)
REPORT_TIME=21:00  # Время отчета менеджеру (МСК)
```

**Примеры настройки:**
- Утренний опрос: `SURVEY_TIME=09:00`
- Обеденный опрос: `SURVEY_TIME=13:30`
- Вечерний отчет: `REPORT_TIME=19:15`

После изменения времени перезапустите бота.

## Структура проекта

```
├── src/
│   └── bot.py           # Основной файл бота с планировщиком
├── data/                # Данные пользователей и ответов
├── reports/             # CSV отчеты для Excel
├── requirements.txt     # Зависимости Python
├── run.bat             # Скрипт запуска для Windows
└── .env                # Переменные окружения
```

## Формат данных

### CSV отчеты
Ежедневно создаются файлы `reports/report_YYYY-MM-DD.csv` с колонками:
- Дата
- Пользователь  
- Настроение
- Проект
- Время ответа

### JSON данные
- `data/users.json` - информация о пользователях
- `data/responses.json` - все ответы по дням