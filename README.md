# Telegram Feedback Bot

Бот для ежедневного сбора обратной связи от сотрудников через Telegram.

## Возможности

- Автоматические ежедневные опросы сотрудников
- Сбор информации о настроении и проектах
- Автоматические отчеты для менеджера
- Экспорт данных в CSV для Excel
- Отправка CSV файлов прямо в Telegram

## Быстрый старт

### 1. Настройка

Скопируйте `.env.example` в `.env` и заполните:

```env
BOT_TOKEN=your_bot_token_here
MANAGER_CHAT_ID=your_chat_id_here
SURVEY_TIME=17:00
REPORT_TIME=21:00
```

### 2. Запуск

```bash
run.bat
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит зависимости
- Запустит бота

## Команды

### Для сотрудников
- `/start` - регистрация
- `/test` - тестовый опрос
- `/help` - справка

### Для администратора
- `/report` - текстовый отчет за сегодня
- `/createreport` - принудительно создать отчет (перезапишет старый)
- `/download` - скачать CSV за сегодня
- `/download YYYY-MM-DD` - скачать CSV за дату
- `/reports` - список всех отчетов
- `/stats` - статистика
- `/schedule` - расписание
- `/help` - справка

**Для удобства:** У администратора есть кнопка "📋 Меню" внизу экрана, которая показывает список всех команд.

## Автоматическая работа

- **SURVEY_TIME** - бот отправляет опрос всем сотрудникам
- **REPORT_TIME** - бот отправляет администратору текстовый отчет + CSV файл

## Структура проекта

```
telegram-feedback-bot/
├── src/
│   └── bot.py              # Основной код
├── data/
│   ├── users.json          # База пользователей
│   └── responses.json      # Ответы
├── reports/                # CSV отчеты
├── .env                    # Конфигурация
├── requirements.txt        # Зависимости
└── run.bat                 # Запуск
```

## Требования

- Python 3.8+
- Telegram Bot Token (получить у @BotFather)
- Chat ID администратора (получить у @userinfobot)

## Формат CSV

| Дата | Пользователь | Настроение | Проект | Время ответа |
|------|--------------|------------|--------|--------------|
| 2026-01-15 | user1 | 👍 Отлично | Проект А | 17:05:23 |

Файлы сохраняются с кодировкой UTF-8-BOM для корректного открытия в Excel.
